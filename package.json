{
  "name": "clomp",
  "version": "1.0.0",
  "private": true,
  "description": "clomp — tamper-evident audit trail for security work (monorepo root)",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.5.2"
  },
  "lint-staged": {
    "backend/src/**/*.js": [
      "eslint --fix --config backend/eslint.config.js"
    ]
  }
}
